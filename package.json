{
  "name": "haraka-plugin-domain-aliases-etcd-config",
  "version": "1.2.0",
  "description": "Haraka plugin that add aliases. It functions the same with domain-aliases plugin, except this one works together with etcd.",
  "main": "index.js",
  "scripts": {
    "lint": "npx eslint *.js test/*.js",
    "lintfix": "npx eslint --fix *.js test/*.js",
    "cover": "NODE_ENV=cov npx nyc --reporter=lcovonly npm run test",
    "versions": "npx dependency-version-checker check",
    "test": "npx mocha"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PartyPancakess/haraka-plugin-domain-aliases-etcd-config.git"
  },
  "keywords": [
    "haraka",
    "plugin",
    "domain-aliases-etcd-config",
    "etcd"
  ],
  "author": "Bilge ONAYCI <bilgehalilonayci@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/PartyPancakess/haraka-plugin-domain-aliases-etcd-config/issues"
  },
  "homepage": "https://github.com/PartyPancakess/haraka-plugin-domain-aliases-etcd-config#readme",
  "devDependencies": {
    "eslint": "*",
    "eslint-plugin-haraka": "*",
    "haraka-test-fixtures": "*",
    "mocha": "*"
  }
}
